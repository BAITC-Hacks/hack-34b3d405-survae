document.getElementById('setup-form').addEventListener('submit',async event=>{event.preventDefault();const status=document.getElementById('setup-status');try{const state=await(await fetch('/api/state')).json();const response=await fetch('/api/setup',{method:'POST',headers:{'Content-Type':'application/json','X-CSRF-Token':state.csrf},body:JSON.stringify({api_key:document.getElementById('api-key').value.trim()})});if(!response.ok)throw new Error((await response.json()).detail);document.getElementById('api-key').value='';location.href='/'}catch(error){status.textContent=error.message}});
