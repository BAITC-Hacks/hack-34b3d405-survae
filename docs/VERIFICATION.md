# Проверки бэкенда

Дата: 23 сентября 2026. Основа: командный репозиторий `BAITC-Hacks/hack-34b3d405-survae`,
исходный коммит `732beba`, локальная ветка `backend/kit-api`.

- `python -m pytest -q`: 39 тестов прошли. Есть предупреждение новой версии Starlette
  о будущем переходе TestClient на httpx2; на результат проверок оно не влияет.
- Реальный HTTP-сервер Uvicorn: GET корзины → пять сообщений уточнения → предложение →
  подтверждение → повторное подтверждение → чтение корзины. Две строки, сумма 10900 KZT,
  повтор не изменяет количество. Другая сессия получает пустую корзину.
- `docs/frontend-client.js` проверен запросами к запущенному серверу.
- API ekt.kz: адаптер прочитал первую страницу (20 позиций) и карточку 515291.
  Цена и городской остаток доступны; расхождение тока в названии/характеристиках обнаружено.
  Данные доступа в проекте не сохранены. Проверка не подтверждает качество всего каталога.
- Источник условий оплаты и доставки: https://ekt.kz/checkout-delivery/ проверен чтением страницы.
  На странице встречаются разные пороги бесплатной доставки; код не обещает бесплатную доставку.
- OpenAI: проверен формат запроса и разбор тестовых ответов/сбоев. Реальный вызов модели
  не выполнялся: нужен личный OPENAI_API_KEY. Не считать это проверкой качества живой модели.
- Совместимый комплект продемонстрирован на явно синтетической системе DEMO-KIT24.
  Комплект из реальных товаров пока не подтверждён: в live-режиме недостающие факты
  возвращаются как нерешённые вопросы, а не заменяются учебными товарами.
- Интеграция записи в реальную корзину ekt.kz отсутствует: API партнёра в материалах только для чтения.
- После объединения: 64 теста pytest прошли, синтаксис app.js и guide.js проверен Node.
- В браузере проверен объединённый сценарий кухни: уточнения кнопками, состав и
  проверки комплекта, пустая корзина до подтверждения и добавление после него.

Эта заметка описывает исходную проверку backend-ветки. Далее frontend и backend объединены
в общей версии; инструкции команды находятся в TEAM_WORKFLOW.md. Настройки GitHub не менялись.
Финальная сдача на платформе хакатона не выполнялась.

Обновление из openai-update.zip: 74 теста pytest проходят. Добавлены проверки
уточнений AI и истории, ответа «нет», обязательного подтверждения, сохранения ключа
только после успешной проверки, обработки ошибок API и сохранения фактов каталога.
Синтаксис трёх изменённых JS-файлов проверен Node. Ключей в архиве не обнаружено;
реальные OpenAI-запросы после объединения не выполнялись.

## Последующая проверка с действующим ключом — 23.09.2026

На программной базе b2a0fd0 ключ проверен реальным запросом, после рестарта
ai_enabled=true, setup_allowed=false. Секрет остаётся только в игнорируемом .env.
74 offline-теста проходят. Полный путь реального AI и demo-комплекта кухни:
engine=openai → предложение 10900 KZT → пустая корзина → явное подтверждение →
две строки/три единицы. Повтор не дублируется; текст «да» корзину не меняет;
другая сессия пуста. Один ответ комплекта занял около 3 секунд.

Браузерный свободный диалог показал дефект BE-01: запрос E27/максимум 40 Вт/3000 К
вернул также лампы 4000 К. Связность компонентов подтверждена, но качество подбора
не признано полностью корректным. Исправление ещё не выполнено, задача передана
в общий план TEAM_CONTEXT.md. Живой ассортимент и реальная корзина магазина этим
тестом не проверялись. Инструкции для трёх участников — TEAM_PROMPTS.md и AGENTS.md.
