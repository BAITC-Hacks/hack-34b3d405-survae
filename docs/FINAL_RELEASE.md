# Финальная сборка для сдачи · 23.09.2026

Собраны main 449628e + backend PR #6 (002394c) + frontend PR #8 (b905c57) + QA PR #7 (5e42248).
Дополнительно обновлён README, сняты xfail с исправленных regression-тестов и добавлен доступный на телефоне «Новый диалог».
Исторические QA-отчёты описывают прежние версии: их красный статус не является статусом этой сборки.

## Проверки

- Обычный `python -m pytest -q`: 173 passed, без skip и xfail; одно предупреждение Starlette/httpx.
- `node --test tests/frontend_flow.test.cjs tests/frontend_consultation.test.cjs`: 6 passed.
- `node --check` для app.js, guide.js и flow.js; `git diff --check`: успешно.
- Исправленные независимые QA-проверки: R1 (партия выбранной лампы), R2 (ровно 40 Вт), R3 (короткий ответ 2 м), BE-02 (две строки Excel).
- Настоящий OpenAI проверен на backend 002394c: 12 сообщений с engine=openai; точные параметры, партия, кухня по коротким ответам и XLSX. До явного подтверждения корзина пуста.
- Финальная общая сборка запускается с теми же backend-файлами; UI доступен, demo и состояние AI обозначены явно. Полный ассортимент ekt.kz и реальный заказ не проверялись.

## Границы результата

Это работающий хакатонный прототип, не production-магазин. 15 синтетических товаров, демонстрационная корзина, сессии в памяти одного процесса. Публичного деплоя нет. Произвольная полная комплектация/совместимость любого оборудования не гарантируется.
Ключи и пользовательские .env не публикуются. Сдача решения на платформе выполняется отдельно от GitHub и должна подтверждаться её статусом.

Порядок запуска и воспроизводимый сценарий — в корневом README.md.
