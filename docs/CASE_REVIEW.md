# QA-01 · соответствие официальному кейсу ekt.kz

Дата проверки: 23.09.2026. Роль: INTEGRATOR / QA. Ветка: `qa/case-review`.
Проверенная база: `0ff1564d047b4175ff79dcd0f1692578971bf4b2`.
Рабочая папка перед обновлением была чистой; main обновлена fast-forward с `23f9020`.
Исходники `app/`, `static/`, каталог и API-контракт в этой задаче не изменены.

**Вывод:** пять основных сценариев показаны на учебных данных, но полное соответствие
продукту ekt.kz не подтверждено. Реальная корзина магазина не подключена. В свободном
поиске нарушаются явные параметры, а при обработке спецификации теряется вторая строка.
74 зелёных теста сами по себе не закрывают эти пробелы.

## Основание и границы

Использован полный текст «HACKALEM AI. Информационный пакет: кейсы ТОО
„Электрокомплект“» и описание API, переданные пользователем в переписке.
Предоставленные ссылки: [документ 1](https://docs.google.com/document/d/172eSfQ4f3D2wgtsfrs8Lonr1SIwAXLx7jZ7hNccISNk/edit),
[документ 2](https://docs.google.com/document/d/1UFgbeZbkn78BCLzZm080zy4IGo7I4p20/edit).
Повторный доступ к содержимому Google Docs в этой проверке не подтверждался;
источник требований — присланный текст. Пароли из пакета здесь не воспроизводятся.

В тексте перечислены пять must-have, входные форматы, ограничения и артефакты.
Числовых весов критериев и точного SLA в миллисекундах нет. Приоритеты P1/P2 ниже —
рекомендация QA, а не баллы организатора. Синтетическая репрезентативная выборка
разрешена кейсом, но это не разрешение называть локальную корзину заказом ekt.kz.

«Пройдено в demo» означает проверку прототипа на синтетических данных.
«Частично» означает наличие реализации с указанным пробелом. «Не проверено»
означает отсутствие достаточного свидетельства, а не доказанное отсутствие функции.

## Пять обязательных требований

| Требование | Что реализовано / результат | Как проверить | Чего не хватает |
| --- | --- | --- | --- |
| M1. Существующий артикул → верные остатки, технические характеристики и сертификат, если он есть в данных | **Пройдено в demo.** `Catalog.detail`, `public_product`, ветка certificate в `app/main.py`. `DEMO-C16-A`: Астана 8 шт., 1 390 ₸, 16 А, ссылка на явно учебный документ. В live отдельно прочитаны страница из 20 товаров и карточка `515291`: артикул `200300285_`, 64 920 ₸, Астана 8; обнаружено расхождение тока в названии и свойствах, показано предупреждение. | `test_known_article_returns_exact_facts_and_certificate`, `test_minimum_party_and_missing_certificate`; в UI «Есть DEMO-C16-A?», открыть документ. Live: сверить `/api/products/detail?id=515291` с нормализованной карточкой. | В проверенной live-карточке сертификатов нет; нужен реальный образец с сертификатом для проверки его отображения. Полный ассортимент не проверен. Свободный подбор по параметрам имеет BE-01; точный поиск артикула проходит. |
| M2. При нулевом остатке минимум один релевантный аналог с кратким обоснованием | **Пройдено в demo.** `Catalog.alternatives` / `analogue_reason`: для `DEMO-C16-Z` возвращаются `DEMO-C16-A` и `DEMO-C16-B`, с совпадающими полюсами, током, напряжением, отключающей способностью и характеристикой C. При недостатке данных — честный отказ. | `test_absent_article_automatically_offers_explained_compatible_analogues`; в UI «Есть DEMO-C16-Z?» → нулевой остаток, две альтернативы, причины. | Нужна подтверждённая пара из реального каталога «нет в наличии → доступный аналог». Отказ при нехватке данных безопасен, но не является успешной демонстрацией требования «минимум один аналог». Кандидаты ограничены выборкой и первыми 16 карточками категории. |
| M3. Содержательные ответы об оплате, доставке и минимальной партии | **Пройдено на контрольных вопросах.** `purchase_conditions` отвечает по опубликованным условиям магазина, отдаёт ссылку на источник. Для минимальной партии учитывается артикул; без него требуется уточнение. | `test_purchase_conditions`, `test_minimum_party_and_missing_certificate`; в UI «Какая оплата?», «Как работает доставка?», «Какая минимальная партия DEMO-CABLE-A?». Для кабеля партия 10, количество 15 отклоняется, 20 допускается. | Условия сохранены в коде: перед сдачей партнёру нужно подтвердить их актуальность; автоматического обновления нет. Не обещать сроки/стоимость доставки для адреса, которых нет в данных. |
| M4. Корзина изменяется только после явного подтверждения, с учётом количества и остатка | **Пройдено для явной кнопки в демо-корзине.** `prepare` → `confirm`; повторная проверка данных, строгий boolean, привязка к сессии, срок 5 минут, атомарность. Текст «да, добавь» сам по себе НЕ добавляет товар: сервер направляет к кнопке. | `test_confirmation_and_idempotency_and_cart_link`, `test_chat_confirmation_and_cancellation`, `test_bad_quantities_cannot_change_cart`, `test_changed_facts_require_new_confirmation`, `test_multi_line_failure_is_atomic`. UI: количество 2 → «В список» → «Проверить набор» → отмена либо «Да, добавь в корзину». До клика count=0, после count=2; запрос 9 при остатке 8 отклоняется. | Буквальный пример из ТЗ с текстовым подтверждением не выполнен: требуется дополнительный клик. Согласовать с постановщиком допустимость кнопки либо с владельцами безопасную поддержку текстового сценария. Реального резервирования остатков и записи в корзину ekt.kz нет. |
| M5. После добавления прямая ссылка на актуальную корзину / оформление | **Пройдено для прототипа.** Ответ содержит `cart.url=/cart`, интерфейс показывает ссылку и текущую корзину той же cookie-сессии. В браузере прямой переход сохраняет 2 × 1 390 = 2 780 ₸. | `test_confirmation_and_idempotency_and_cart_link`; после добавления открыть `/cart` напрямую в том же браузере, затем обновить. Проверить уведомление «заказы на ekt.kz не оформляются». | `/cart` относится к этому приложению, а не к корзине магазина. Нет оформления реального заказа; другой браузер имеет отдельную корзину. Перезапуск процесса очищает её. Нужен официальный интерфейс записи и согласование интеграции с партнёром. |

## Остальные требования основного текста

| Требование | Что реализовано / результат | Как проверить | Чего не хватает |
| --- | --- | --- | --- |
| Единое окно чата, свободный текст, контекст сессии | **Частично.** Чат, история последних сообщений, уточнения AI; локальные правила без ключа, обозначенный fallback при ошибке. Отдельный мастер кухни работает по ограниченному сценарию. | `test_ai_clarification_keeps_history_and_empty_cart`, тесты комплектования; свободный диалог и новый диалог без очистки корзины. Реальный AI-запрос к `DEMO-C16-A` получил `engine=openai`. | BE-01: свободный поиск не гарантирует соблюдение всех явных характеристик. Нет большого набора проверенных живых диалогов, поэтому не заявлять универсальный подбор. |
| Вложения: Excel, Word, PDF, JPEG / фото спецификации | **Частично.** `/api/upload`, `attachments.extract`: XLSX/XLS, DOCX, PDF с текстовым слоем, JPEG/PNG; дополнительно TXT/CSV. Лимит 8 МБ, PDF до 30 страниц. Для фото нужен локально настроенный AI. | `test_text_and_office_attachments`, `test_jpeg_and_invalid_files`; новая приёмка загружает настоящий XLSX из двух синтетических строк через HTTP и проверяет дальнейшее предложение. | **BE-02:** обе строки извлечены, но обработчик игнорирует `plan.items` и предлагает только первую. Legacy `.doc` не поддерживается; PDF-сканы требуют JPEG. Полный путь «реальное фото/PDF/XLS → верные артикулы и количества → подтверждение» не проверен. Кейс не уточняет обязательность `.doc`, это вопрос о границе Word, а не придуманное требование. |
| Каталог и остатки со структурой полей; разрешена репрезентативная синтетическая выборка | **Есть.** 15 demo-позиций: артикул, категория, характеристики, цена, складские остатки, документы при наличии. Live-адаптер читает страницы и детали; стандартная настройка — до 400 позиций. Перед confirm детали запрашиваются заново. | Проверить `catalog.mode/count/error` в `/api/state`; `test_conflicting_catalog_characteristics_flagged`, тесты live-без-demo; сопоставить live-поля с API партнёра. | До 400 — настройка выборки, не доказательство полноты ассортимента. В этом аудите live проверен на одной странице и одной карточке. Требуется согласованная выборка с достаточными характеристиками и аналогами. |
| Ответ за «единицы секунд» | **Один положительный замер:** 4,07 с для реального OpenAI + demo-каталога, HTTP 200; карточка и остаток корректны, корзина не изменена. | На этой машине один запрос «Есть DEMO-C16-A? Покажи характеристики и наличие.» через TestClient с настоящим провайдером. Для приёмки производительности отдельно измерить серию запросов и медленный/недоступный провайдер. | Это не SLA и не нагрузочный тест. Обычный AI-ответ может делать два последовательных внешних запроса; настроенные таймауты допускают заметно более долгий ответ. Нет данных p50/p95 и UX-проверки всей серии отказов. |
| Не сообщать недостоверные цены и наличие, объяснять аналоги | **Частично доказано.** Карточки и корзина берут факты из каталога; неизвестные значения не подменяются нулём. Аналоги имеют объяснения. Коммерческие действия не доверяются модели. | Тесты изменения цены/остатка, некорректных данных, внедрения инструкций; сравнить карточку и источник. | Новое свободное `compose_reply` добавляется к ответу без проверки всех фактов в тексте. Сохранение правильной карточки не доказывает отсутствие выдумок в пояснении. Нужны проверки ответа AI на противоречия; успешный один запрос не доказывает устойчивость. |
| Не изменять корзину/заказ без подтверждения; защищать от чужих изменений | **Пройдено в проверенных сценариях.** CSRF, допустимые origin, изолированные cookie-сессии, строгий confirm, повтор без дублей; удаление и очистка также требуют подтверждения. | `test_session_isolation_csrf_and_origin`, `test_clear_and_remove_require_confirmation`, `test_prompt_injection_cannot_confirm_or_supply_price`, backend-тесты чужого/старого предложения. | Это проверка однопроцессного прототипа, не аудит production-защиты. Для интеграции в реальный сайт нужны модель авторизации, общий session-store и проверка на площадке партнёра. |
| Приватность; не запрашивать/хранить платёжные данные | **Частично.** Платёжной формы нет, ассистент не запрашивает реквизиты. Вложения/история временно находятся в памяти, секреты на сервере; README объясняет передачу запроса и вложения OpenAI с `store=false`. | Проверка `upload`, хранения сессии, `purchase_conditions`, настройки cookie; проверить отсутствие ключей в frontend и отслеживаемых `.env`. | Нет фильтра случайно присланных платёжных данных: произвольный текст может попасть в историю/к провайдеру. Нет явной очистки неактивных серверных сессий по TTL; cookie max-age не равен удалению памяти. `store=false` не является доказательством нулевого хранения у провайдера. До публичной интеграции определить политику удаления и обработки таких входов. |
| Совместимость с текущей платформой сайта ekt.kz | **Не подтверждена.** Есть самостоятельный FastAPI-сайт, три согласованных API-метода и правила CORS для разработки. | `docs/FRONTEND_API.md`; договориться с партнёром о точке встраивания, авторизации, cookie и cart API, затем пройти сценарий в тестовой среде его сайта. | Нет доказанного встраивания на текущую платформу, общего логина или реальной корзины. Запуск на localhost не является интеграцией в ekt.kz. |
| Десктопная и мобильная версии | **Пройден ограниченный smoke.** На 1280×800 показаны карточка и подготовка предложения; на 390×844 выполнены отмена/подтверждение, просмотр корзины, аналогов и оплаты. Страница корзины: ширина документа 390 при viewport 390. | Ручной браузерный сценарий ниже; проверить доступность кнопок с прокруткой и видимое уведомление demo. | Это эмуляция размеров в одном браузере, не проверка физических iOS/Android, всех экранов, загрузки файлов и доступности. На узком экране кнопка корзины озвучивается только числом — задача FE-01. |
| Репозиторий и README: архитектура, запуск, данные | **Пройдено на macOS/Python 3.13.** Чистый клон текущего Git, новая `.venv`, установка requirements-dev, 74 теста, `pip check`, запуск без ключей. README содержит архитектуру и происхождение данных. | Команды в разделе воспроизводимости ниже; API `/api/state`: версия `0ff1564`, 15 demo-позиций, `ai_enabled=false`, error=null. | Windows/Linux не запускались. Зависимости заданы диапазонами, версии могут изменяться; свежая установка дала предупреждение Starlette о TestClient/httpx. Некоторые абзацы README об отсутствии живой проверки AI устарели; актуальный результат находится в этом отчёте. При повторном запуске не перезаписывать существующий `.env`. |

## Опциональные пункты — не смешивать с must-have

| Пункт | Состояние | Проверка / пробел |
| --- | --- | --- |
| Казахский язык | Частично в схеме AI, полноценная поддержка не доказана | Есть `language=kk` и отдельные распознаваемые фразы, но основные ответы и `compose_reply` ориентированы на русский. Нужен сценарий на казахском. |
| Сопутствующие товары | Ограниченный комплект кухни | Тесты проверяют модули и блок питания; это не универсальная система рекомендаций. |
| История для авторизованных пользователей | Нет подтверждённой реализации | Только история анонимной cookie-сессии в памяти; логина и постоянного хранения нет. |
| Эскалация менеджеру | Есть текстовое направление | Нет проверенного канала передачи диалога сотруднику. Не выдавать совет «обратитесь к менеджеру» за интеграцию. |

## Воспроизводимость и фактические результаты

1. Создан отдельный чистый клон **из Git main `0ff1564`**, без старых ZIP,
   пользовательского `.env`, кэша и существующей `.venv`.
2. Python **3.13**, macOS. Новая `.venv`; установка `requirements-dev.txt` включает
   `requirements.txt`. Первоначально сеть песочницы блокировала установку;
   после разрешения сети установка из PyPI завершилась. Это ограничение среды, не баг проекта.
3. В чистом окружении: **74 passed, 1 warning за 2,37 с**; `pip check`:
   `No broken requirements found`. Предупреждение: Starlette рекомендует `httpx2`
   вместо текущего `httpx` для TestClient. Зависимости приложения здесь не менялись.
4. Скопирован только `.env.example` в отсутствующий `.env`; запущен один uvicorn
   на **127.0.0.1:8001** для изолированной QA-проверки. Без платного аккаунта:
   demo=15, OpenAI выключен, загрузка завершена, ошибок каталога нет.
5. В браузере: точный артикул → количество 2 → список → предложение → отмена
   (корзина 0) → новое предложение → явная кнопка подтверждения (корзина 2,
   2 780 ₸) → прямая `/cart` с тем же составом. Затем проверены нулевой остаток,
   две объяснённые альтернативы и ответ об оплате со ссылкой на источник.
6. В основной локальной копии отдельно проверены **наличие настройки** OpenAI
   и EKT, без вывода значений секретов. Один реальный AI-запрос на синтетических
   данных: HTTP 200, `engine=openai`, 4,07 с. API EKT: одна страница и карточка,
   результаты M1. Никаких реальных заказов/изменений ekt.kz не выполнялось.
7. Два новых приёмочных теста воспроизводят BE-01/BE-02. Для BE-02 XLSX реально
   загружается и извлекается; ответ разбора AI подставлен, чтобы доказать потерю
   строки **после** успешного распознавания, независимо от качества модели.
   Обычный запуск показывает `xfail`, а не успешную приёмку. С `--runxfail`
   оба теста падают именно на неверном наборе товаров/пропавшей строке.
8. По списку отслеживаемых файлов не найдены `.env`, `.venv`, кэши, `.pyc`,
   `.DS_Store` или ZIP. Это не полный поиск секретов во всей истории Git.

Команды из корня новой копии (не копировать поверх рабочего `.env`):

```bash
python3 -m venv .venv
.venv/bin/python -m pip install -r requirements-dev.txt
.venv/bin/python -m pip check
.venv/bin/python -m pytest -q -rx
test -f .env || cp .env.example .env
.venv/bin/python -m uvicorn app.main:app --host 127.0.0.1 --port 8001
```

Для независимого запуска без ключей оставьте значения из `.env.example` и не
передавайте секреты окружением. В этой ветке итог тестов должен явно различать
**74 passed и 2 xfailed**. Чтобы увидеть подтверждённые дефекты:

```bash
.venv/bin/python -m pytest -q tests/test_acceptance_case.py --runxfail
```

Ожидается **2 failed** до исправления. Маркеры `strict=True` требуют убрать xfail
после исправления: неожиданный успех станет XPASS и остановит обычную проверку.

## Передача владельцам и очередь интеграции

Снимок GitHub в **15:51 Asia/Almaty, 23.09.2026**: main `0ff1564`, **0 открытых PR**,
4 закрытых. Это разовая проверка, не мониторинг. Незапушенная работа других ноутбуков
не видна. Никто не назначен автоматически и никакие PR в этой задаче не объединялись.

| ID / приоритет QA | Предложенный владелец | Следующий проверяемый результат |
| --- | --- | --- |
| BE-01 / P1, подтверждено | BACKEND | Явные цоколь/температура/мощность отсекают неподходящие позиции. `test_explicit_lamp_parameters_exclude_incompatible_results` проходит без xfail; дополнительно 5–10 диалогов. Изменение API согласовать с FRONTEND. |
| BE-02 / P1, подтверждено | BACKEND | Все распознанные строки спецификации сохраняются с количеством либо показываются как нерешённые; ничего не пропадает молча. `test_two_line_excel_specification_preserves_both_items` проходит без xfail, подтверждение всё ещё обязательно. |
| INT-01 / P1, решение по границе интеграции | INTEGRATOR + партнёр | Зафиксировать, принимается ли локальная демо-корзина на защите, допустима ли кнопка вместо текстового «да, добавь»; при требовании реальной корзины получить официальный cart API и тестовую среду. Не имитировать успешный заказ. |
| DATA-01 / P1 для live-показа | BACKEND + партнёр | Предоставить проверяемые реальные позиции: сертификат и пара «отсутствующий товар → аналог» с нужными характеристиками. |
| AI-01 / P1, риск требует проверки | BACKEND + QA | Проверить свободные пояснения на ложные цену/наличие/совместимость; правильная карточка не компенсирует неверный текст. |
| FE-01 / P2 | FRONTEND | Понятные состояния ошибок/fallback, результаты всех строк файла, видимые ограничения demo; доступное название кнопки корзины на мобильном. |
| QA-02 / P1 перед сдачей | INTEGRATOR | После согласованных PR повторить чистый запуск и весь сценарий уже на итоговом SHA; отдельно серия замеров задержки, фото/PDF/XLS, браузерные ошибки. |
| PRIV-01 / P2 до публичного запуска | BACKEND + INTEGRATOR | Определить удаление неактивных сессий и обработку случайно присланных платёжных данных; показать пользователю, какие вложения уходят AI. |

Порядок: сначала исправить BE-01/BE-02 и определить INT-01, затем проверка на
репрезентативных данных и итоговый QA-02. Мерж — после согласования владельцев,
проверок и разрешения пользователя. Этот отчёт не является финальной сдачей на платформу.
